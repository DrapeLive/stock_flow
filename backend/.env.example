DEBUG=False
SECRET_KEY=
ALLOWED_HOSTS=api.yourdomain.tld
CORS_ALLOWED_ORIGINS=https://stockflow-sigma.vercel.app

DB_NAME=stockflow
DB_USER=stockflow
DB_PASSWORD=change-me
DB_HOST=db
DB_PORT=5432

# Mirror of DB_* — consumed by the postgres container
POSTGRES_DB=stockflow
POSTGRES_USER=stockflow
POSTGRES_PASSWORD=change-me

# For reset password - SMTP
EMAIL_HOST="smtp-host"
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER="smtip-user"
EMAIL_HOST_PASSWORD="smtp-password"
DEFAULT_FROM_EMAIL="from-email"

# For web push notification
PUBLIC_VAPID_KEY=
PRIVATE_VAPID_KEY=
CELERY_BROKER_URL=
